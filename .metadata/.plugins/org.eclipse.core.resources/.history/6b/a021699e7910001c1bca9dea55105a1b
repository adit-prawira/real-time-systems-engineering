#include <stdio.h>
#include <stdlib.h>
#include <errno.h>
#include <sys/iofunc.h>
#include <sys/netmgr.h>

#define BUF_SIZE 100

typedef struct{
	struct _pulse header;
	int clientId;
	int data;
}messageData;

typedef struct {
	struct _pulse header;
	char buf[BUF_SZIE];
}reply;
int main(void) {
	puts("Hello World!!!"); /* prints Hello World!!! */
	return EXIT_SUCCESS;
}
