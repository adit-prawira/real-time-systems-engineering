#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>
int dataReadyCount = 0;

// utilize default attributes to initialize mutex and condvar
pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;
pthread

int main(void) {
	puts("Hello World!!!"); /* prints Hello World!!! */
	return EXIT_SUCCESS;
}
